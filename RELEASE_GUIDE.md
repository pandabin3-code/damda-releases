# 담다 DAMDA 배포 안내 · 0.5.0-beta.3

Daily Assistant for Messaging & Data Archive

## 설치와 업데이트

- 권장 파일: `Damda-WebView-Preview-Setup-0.5.0-beta.3-x64.exe`. 일반 프로그램 창으로 실행합니다.
- 대체 파일: `Damda-Setup-0.5.0-beta.3-x64.exe`. 기본 브라우저에서 실행합니다. 두 버전은 같은 사용자 업무를 사용하므로 동시에 실행하지 않습니다.
- Windows 64비트용입니다. Python을 따로 설치할 필요가 없습니다. WebView 버전은 Microsoft WebView2 런타임이 필요하며 설치 마법사가 런타임 설치를 지원합니다. 런타임이 없는 PC는 인터넷 연결이 필요합니다.
- 편집 내용을 저장한 뒤 새 설치 파일을 실행하세요. 설치 프로그램이 실행 중인 담다에 정상 종료를 요청하고 파일 잠금 해제를 기다립니다. 가져오기나 동기화가 진행 중이면 완료 후 다시 설치하세요. 강제 종료로 저장을 건너뛰지 않습니다.
- 위 자동 종료 요청은 권장 WebView 설치본의 동작입니다. 브라우저 대체 설치본은 앱의 `앱 종료`를 먼저 눌러야 합니다. 브라우저 탭만 닫으면 백엔드가 남을 수 있습니다.
- 앱 시작 시와 사용 중 약 6시간 간격으로 새 버전을 확인합니다. 새 버전이 있으면 알림과 다운로드 링크를 표시합니다. 다운로드와 설치는 사용자가 시작합니다. 자동으로 강제 업데이트하지 않습니다.
- 업무와 설정은 설치 폴더 밖에 보관합니다. 재설치·업데이트·일반 제거는 업무 초기화가 아닙니다. 초기 화면 검증은 별도의 첫 실행 테스트 기능을 사용하세요.
- 베타 설치 파일에는 코드 서명이 없습니다. 조직의 실행 제한이나 Windows 보안 정책 때문에 설치가 차단될 수 있습니다. 승인된 배포 정책을 따르세요.

## 다른 PC에서 달라지는 부분

- 쿨메신저 메시지함은 각 PC에서 기본 위치를 찾습니다. 다른 위치에 설치했다면 설정에서 변경합니다. 개발자의 경로를 기본값으로 배포하지 않습니다.
- 첨부파일 기본 위치는 해당 Windows 사용자의 문서 폴더 안 `CoolMessenger Files / Received Files`입니다. 문서 폴더가 이동된 환경도 사용자의 문서 위치를 따릅니다. 설정에서 변경할 수 있습니다.
- 자동 가져오기 작업은 PC마다 최초 한 번 연결해야 합니다. 관리자 계정의 UAC 승인으로 등록한 뒤 주기적 가져오기에는 별도 승인 창을 띄우지 않습니다. 일반 사용자 계정에서 다른 관리자 자격 증명을 쓰는 경우는 같은 흐름을 보장하지 않으며 전산 담당자 설정이 필요할 수 있습니다.
- 쿨메신저가 없는 PC에서도 업무를 직접 만들 수 있습니다. 연결을 강제하지 않습니다.
- 학교 방화벽은 GitHub, Google, 날씨 제공처, 나이스 접속을 제한할 수 있습니다. 연결 실패를 최신 버전 또는 급식 없음으로 표시하지 않습니다.
- 저장 내용은 현재 Windows 사용자 계정에 연결해 암호화합니다. 파일 복사만으로 다른 PC에서 복원하는 기능은 제공하지 않습니다. 모바일 읽음 상태는 검증 유예 상태이며 보존을 보장하지 않습니다.

## Google 캘린더 · beta.1에서 추가

오른쪽 `📅 Google 캘린더`에서 연결합니다. 현재는 **담다 → Google 단방향 수동 동기화**입니다. 제목·마감일·시각을 확인한 업무만 선택해서 등록하거나 갱신합니다. 원문, 첨부파일, 발신자 필드를 자동 전송하지 않습니다. 제목 자체에 개인정보를 적었다면 전송 전에 확인하세요.

Google Cloud에서 Calendar API를 활성화하고 OAuth 동의 화면과 **데스크톱 앱** 클라이언트를 구성해야 합니다. 앱의 최초 연결 설정에 다운로드한 JSON 경로를 입력한 뒤 Google 로그인을 누릅니다. 로그인은 시스템 브라우저에서 진행하며 PKCE와 임의 포트의 로컬 콜백을 사용합니다. 테스트 모드의 사용자 제한·인증 만료, 학교 계정의 관리 정책이 적용될 수 있습니다. 공개 배포용 OAuth 동의 화면과 필요한 Google 검증은 배포자가 별도로 준비해야 합니다.

현재 배포에는 개발자 OAuth JSON이나 사용자 토큰이 포함되지 않습니다. 실제 계정 로그인·일정 등록은 계정 설정이 없어 아직 검증하지 않았습니다. 합성 테스트로 중복 ID, 재시도, ETag 충돌, 선택 확인 및 원문 미전송 경로를 검증했습니다.

연결을 해제해도 이미 등록한 일정은 삭제하지 않습니다. Google에서 삭제·변경한 일정은 무조건 복원하거나 덮어쓰지 않습니다. 완료·삭제 상태의 양방향 동기화는 제공하지 않습니다.

공식 문서: https://developers.google.com/identity/protocols/oauth2/native-app

## 학교 급식 · beta.2에서 추가

오른쪽 `🍽 학교 급식`에서 나이스 인증키를 저장하고 학교 이름을 검색해 선택합니다. 주간 식단·칼로리·영양 정보를 조회하며 알레르기 표시는 유지합니다. 조회 결과는 실행 중 최대 12주, 6시간 동안 캐시합니다. 앱을 다시 실행하면 캐시를 새로 만듭니다.

인증키 없는 응답은 5건 제한의 시험용이므로 정식 식단 화면에 사용하지 않습니다. 실제 키가 없어 실학교 조회는 아직 검증하지 않았습니다. API 파싱·학교 선택·빈 결과·접속 실패·이전 결과 표시를 합성 응답으로 검증했습니다.

공식 안내: https://open.neis.go.kr/portal/guide/apiGuidePage.do

## 요약 교정

설정의 `✍ 요약 함께 교정`으로 열립니다. 준비된 교정 자료가 있는 PC에서 후보 선택·직접 작성·분류 변경·자동 중간 저장·부분 제출을 할 수 있습니다. 선택 결과는 다음 후보의 추천 순서에 반영됩니다. 확정 답변은 후속 알고리즘 교정을 위한 자료이며, 자동으로 학습을 마쳤다는 뜻은 아닙니다.

이번 작업의 실제 메시지 200건은 요청자의 PC에만 암호화해 저장했습니다. 공개 설치 파일과 소개 ZIP에는 포함하지 않습니다. 부분 제출 시 만든 JSON에는 사용자가 확정한 문구가 들어 있으므로 공개 GitHub 오류 보고에 첨부하지 마세요.

## 오류 보고

앱의 오류 보내기에서 설명을 입력하고 보고서를 준비하면 GitHub 작성 화면으로 연결됩니다. 사용자가 확인하여 제출해야 공개 이슈가 생성됩니다. 진단 정보는 앱 버전·허용된 상태 값 중심이며 메시지 원문과 발신자 명단을 자동 수집하지 않습니다.
## 0.5.0-beta.4 업데이트 흐름

이 버전에는 D+체크 아이콘, 메인 메뉴의 버전 표시, 날짜 위 달력 점, 자동 업데이트 버튼이 추가됩니다. 공개 설치 파일명은 `Damda-WebView-Preview-Setup-0.5.0-beta.4-x64.exe` 및 `Damda-Setup-0.5.0-beta.4-x64.exe`입니다.

beta.3 이하에서 beta.4로 올라오는 첫 설치는 기존 다운로드 방식으로 실행합니다. beta.4부터 이후 버전은 자동 업데이트 버튼으로 공식 GitHub 파일 다운로드·SHA-256 검증·정상 종료·설치·재실행을 이어갑니다. 편집 중이면 먼저 저장해야 하고, 가져오기/캘린더 동기화 중이면 설치를 보류합니다. 사용자의 실제 저장 폴더를 재실행 인수로 유지합니다. 검증 불일치나 다운로드 실패 시 설치하지 않습니다. 임의의 클라이언트 제공 실행 파일 경로나 URL을 실행하지 않습니다.

서비스 등록이 필요한 Google 로그인/급식 간편 연결은 별도 준비 중입니다. 아래 beta.3의 최초 설정 관련 설명은 아직 해당 기능의 현재 제약을 설명합니다.
